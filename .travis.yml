language: cpp

sudo: false

os:
 - linux
 - osx

compiler:
 - gcc
 - clang

env:
 -
 - CONFIGURE_FLAGS="--enable-shared"

matrix:
  include:
    - services:
       - docker
      env:
       - DOCKER=i386/ubuntu

addons:
  apt_packages:
      - autoconf
      - automake
      - libtool


script: ./.travis-build.sh

after_failure:
  - ./.travis-print-logs.sh

cache: ccache
